# Quick-start: Run minimal_case
---

!!! important
	Follow the installation guide before starting here

To immediately run the pre-set minimal_case case study, you can run the following CLI command from the repository's root:

```
uv run python -m model.main --config config.yaml      
```

